exports.Capitalize = function(s) {
    res = ""
    for (i = 0; i < s.length; i++) {
        if (i % 2 == 0)
            res += s.charAt(i).toUpperCase();
        else
            res += s[i];
    }
    return (res);
}